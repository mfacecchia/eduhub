const LandingPage = () => {
    document.title = import.meta.env.VITE_PAGE_TITLE;

    return (
        <section>
            <h1>Landing page</h1>
        </section>
    );
};

export default LandingPage;
